@page
@model MoneyManagementApp.Pages.User.IndexModel

<p> 
    <a asp-page="Create">Create new</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Saver[0].Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Saver[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Saver[0].Password)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Saver[0].Avatar)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.Saver) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Password)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Avatar)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.UserId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.UserId">Details</a>
            </td>
        </tr>
}
    </tbody>
</table>
